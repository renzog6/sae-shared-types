# 🧱 SAE Shared Types

**Shared TypeScript definitions for the SAE system (backend + frontend).**  
Developed for **RCM SA**.

---

## 🧩 Overview

This package provides a unified source of truth for all TypeScript interfaces, DTOs, and schemas used by both the SAE backend (NestJS) and frontend (Next.js).

---

## 🧠 Usage

### In the backend or frontend:

```bash
npm install git+ssh://git@github.com/tu-org/sae-shared-types.git
```
